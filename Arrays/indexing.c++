#include <iostream>
using namespace std;
int main (){
    // Most programming languages, including C++, Java, and Python, use zero-based indexing. This means that the first element of an array is accessed with index 0, the second element with index 1, and so on.
  int arr[5] = {10, 20, 30, 40, 50};
  int firstElement = arr[0]; // Accesses 10
  cout<<firstElement<<endl;
}